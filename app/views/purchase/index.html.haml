%h2 購入を確定しますか？
-# = form_with model: @production, url: pay_purchase_index_path, method: :post do |f|
-# - @production.each do |production|
-#   = production.name
-#   = production.price
= @production.name
= @production.price
%p Apple MacBook Pro 13インチ
%p ¥135,000(送料込み)
%br
%h3 支払い方法
- if @card.blank?
  %br /
- else
  -#以下カード情報を表示
  = "**** **** **** " + @card[:last4]
  - exp_month = @card.exp_month.to_s
  - exp_year = @card.exp_year.to_s.slice(2,3)
  = exp_month + " / " + exp_year
%br
= form_tag(action: :pay, method: :post) do
  %button 購入する
